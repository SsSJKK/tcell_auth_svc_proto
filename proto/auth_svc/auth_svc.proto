syntax = "proto3";

package auth;

option go_package = "sssjkk.auth.v1;auth1";

service Auth { 
  rpc SignUp (SignUpRequest) returns (SignUpResponse); 
  rpc SignIn (SignInRequest) returns (SignInResponse);
  rpc RefreshToekn (RefreshToeknRequest) returns (RefreshToeknResponse);
  rpc CheckToken (CheckTokenRequest) returns (CheckTokenResponse);
  rpc TakePin (TakePinRequest) returns (TakePinResponse);

}

message SignUpRequest {
  string login = 1; 
  string password = 2;
  int32  app_id = 3;
}

message GetTokenRequest {
  string login = 1; 
  int32  app_id = 2;
}
message TakePinRequest {
  string login = 1; 
  int32  app_id = 2;
}

message SignUpResponse {
  int64 user_id = 1; 
}
message RefreshToeknRequest {
  string refresh_token = 1; 
}
message CheckTokenRequest {
  string refresh_token = 1; 
}
message CheckTokenResponse {
  int64 user_id = 1; 
}
message TakePinResponse {
  int64 user_id = 1; 
}

message SignInRequest {
  string login = 1; 
  string password = 2;  
  int32  app_id = 3;
}

message SignInResponse {
  string access_token = 1; 
  string refresh_token = 2; 
}
message RefreshToeknResponse {
  string access_token = 1; 
  string refresh_token = 2; 
}
